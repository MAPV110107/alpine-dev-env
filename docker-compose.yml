services:
  memcached:
    image: memcached:alpine
    restart: unless-stopped
  yopass:
    image: jhaals/yopass
    ports:
      - "1337:1337"
    command: --memcached=memcached:11211
    restart: unless-stopped
    depends_on:
      - memcached